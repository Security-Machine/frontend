console.log(
    '==================================================\n' +
    '=========[ Setting up tests for monorepo ]========\n' +
    '==================================================\n'
);

// This is the URL of the auth server.
process.env.NX_AUTH_URL = 'auth123';
