import { SignUpMuiForm } from '@secma/mui';

/**
 * Renders the sign-up page, allowing the user to enter the credentials.
 *
 * When the user is signed in, the page redirects to the page set in the
 * navigation state or to the default page.
 */
export const SignUpPage = SignUpMuiForm;
